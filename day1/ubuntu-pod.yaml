apiVersion: v1
kind: Pod
metadata:
  namespace: default
  labels:
    run: ubuntu-pod
    app: ubuntu 
  annotations:
    app-description: "testowy pod ubuntu, ktory failuje"
  name: ubuntu-pod
spec:
  containers:
  - image: ubuntu
    name: ubuntu-pod
    command: ['sh', '-c' ,'tail -F /tmp/test']
